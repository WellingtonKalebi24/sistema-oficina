---
phase: 01-funda-o-t-cnica-e-contrato-visual
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .gitignore
  - .env.example
  - tsconfig.base.json
  - eslint.config.mjs
  - .prettierrc
  - .prettierignore
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/api/package.json
  - apps/api/tsconfig.json
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
  - tests/walking-skeleton.test.ts
autonomous: false
requirements:
  - FND-01
  - FND-03
  - FND-08
requirements_addressed:
  - FND-01
  - FND-03
  - FND-08
must_haves:
  truths:
    - "Root npm workspace scripts exist for lint, format check, type check, tests and full verification per D-02, D-04 and D-05."
    - "D-03: Frontend and backend workspace apps remain runnable independently through their own scripts, while later plans also wire Docker Compose execution."
    - "The first walking-skeleton test describes the full UI to API to database happy path before implementation per D-06."
    - "Real secrets remain untracked and only documented sample variables are committed per D-10."
  artifacts:
    - "package.json"
    - "package-lock.json"
    - ".env.example"
    - "tsconfig.base.json"
    - "eslint.config.mjs"
    - "tests/walking-skeleton.test.ts"
  key_links:
    - "Root scripts must call workspace scripts consistently."
    - "Workspace package names must match script targets used by later plans."
---

<objective>
Create the gated npm workspace scaffold and the first failing walking-skeleton verification contract.

Purpose: Establish the executable project surface without adding business modules, while honoring D-01 through D-07 and D-10.
Output: Root workspace metadata, strict TypeScript and quality tooling configuration, environment example, and a failing end-to-end contract test that later plans make pass.
</objective>

<execution_context>
@E:/sistema_oficina2/.codex/gsd-core/workflows/execute-plan.md
@E:/sistema_oficina2/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@PROJETO.md
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-funda-o-t-cnica-e-contrato-visual/01-CONTEXT.md
@.planning/phases/01-funda-o-t-cnica-e-contrato-visual/01-RESEARCH.md
</context>

<must_haves>
- FND-01: The repository declares npm workspaces for `apps/web`, `apps/api` and `packages/shared` per D-01, D-02 and D-03.
- FND-03: Root scripts named `lint`, `format:check`, `typecheck`, `test` and `verify` fan out to workspaces per D-04.
- FND-03: TypeScript configuration starts strict for every workspace per D-05.
- FND-08: `.env.example` lists required local variables without committing real secrets per D-10.
- FND-03 and D-06: `tests/walking-skeleton.test.ts` encodes the expected full-stack proof: Docker services, migration, seed, `/health`, a foundation write/read API, and a web interaction.
- D-07 and D-15: The scaffold contains no permanent mock data and no communication or notification-shaped domain entities.
</must_haves>

<tasks>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Approve SUS package legitimacy gate</name>
  <files>package.json, package-lock.json</files>
  <what-built>The phase research package audit marked these standard packages as SUS due to publish recency: `vite`, `@vitejs/plugin-react`, `typescript`, `@types/node`, `tsx`, `helmet`, `vitest`, `eslint`, `typescript-eslint`, and `prettier`.</what-built>
  <action>Pause before package installation so the developer can verify the SUS package names, publishers and source repositories required by the package legitimacy gate. Continue only after the explicit resume signal.</action>
  <how-to-verify>
    1. Open the npm package pages for the listed packages.
    2. Confirm each package name, publisher/source repository, and legitimacy match the Phase 1 research audit.
    3. Confirm no package is being substituted by a similarly named package.
  </how-to-verify>
  <verify>
    <automated>MISSING — blocking human package legitimacy checkpoint must be approved before `npm install` changes package files.</automated>
  </verify>
  <done>Developer has approved the listed SUS package identities and package installation may proceed.</done>
  <resume-signal>Type "approved packages" to continue package installation, or name the package concern to revise.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Create workspace quality scaffold and failing skeleton contract</name>
  <files>package.json, package-lock.json, .gitignore, .env.example, tsconfig.base.json, eslint.config.mjs, .prettierrc, .prettierignore, apps/web/package.json, apps/web/tsconfig.json, apps/api/package.json, apps/api/tsconfig.json, packages/shared/package.json, packages/shared/tsconfig.json, packages/shared/src/index.ts, tests/walking-skeleton.test.ts</files>
  <behavior>
    - Test 1: The walking-skeleton contract fails before implementation because no API, database route, Docker services, or web interaction exists yet.
    - Test 2: Root verification scripts are discoverable and target the declared workspaces.
    - Test 3: Environment documentation exposes sample values only and never requires a committed real `.env`.
  </behavior>
  <action>After the package checkpoint is approved, create npm workspaces per D-01 and D-02 with package names for `apps/web`, `apps/api`, and `packages/shared`. Install the audited packages from `01-RESEARCH.md`, generate `package-lock.json`, and define root scripts for `lint`, `format:check`, `typecheck`, `test`, `verify`, `db:migrate`, `db:seed`, and Docker smoke commands per D-04. Add strict shared TypeScript settings per D-05, ESLint flat config, Prettier config, `.gitignore` entries for `.env`, build outputs and dependencies, and `.env.example` per D-10. Add `tests/walking-skeleton.test.ts` as the first RED contract for the Phase 1 happy path per D-06: it must require a clean migration, deterministic seed, health response with database connected, a write/read foundation API, and a web action that triggers that API. The RED test must fail with a stable expected missing-implementation diagnostic such as `EXPECTED_MISSING_IMPLEMENTATION` until Plans 02 and 03 provide the API, database and web implementation; syntax errors, missing test runner setup, or unrelated runtime failures are not acceptable RED results. Keep the model name neutral, such as `FoundationCheck`, and do not create auth, tenant, customer, stock, quote, work order, finance, portal, report, dashboard, notification, message or communication entities per D-07 and D-15.</action>
  <verify>
    <automated>$output = npm run test -- tests/walking-skeleton.test.ts 2&gt;&amp;1; $code = $LASTEXITCODE; $text = $output -join "`n"; if ($code -eq 0) { Write-Error "Expected walking skeleton contract to be RED before implementation"; exit 1 }; if ($text -match "EXPECTED_MISSING_IMPLEMENTATION") { exit 0 }; Write-Output $text; Write-Error "Walking skeleton contract failed for an unexpected reason"; exit 1</automated>
    <automated>npm run format:check</automated>
    <automated>rg -n "Tenant|User|Customer|Vehicle|Product|Quote|WorkOrder|Payment|Notification|MessageQueue|WhatsAppIntegration|EmailIntegration" package.json apps packages tests -g "!package-lock.json"; $code = $LASTEXITCODE; if ($code -eq 0) { Write-Error "Forbidden later-phase or notification domain found in Plan 01 scaffold"; exit 1 }; if ($code -eq 1) { exit 0 }; Write-Error "Forbidden-domain scan failed with rg exit code $code"; exit $code</automated>
  </verify>
  <done>Root workspace and quality scripts exist, strict TypeScript is configured, `.env.example` exists without secrets, and the walking-skeleton test fails for missing implementation rather than syntax or setup errors.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine to npm registry | Package manager downloads executable build/test dependencies. |
| Local env files to application runtime | Environment variables cross from developer configuration into API, Prisma and Docker. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01-SC | Tampering | npm package installation | high | mitigate | Blocking package legitimacy checkpoint before installing SUS packages, then commit `package-lock.json`. |
| T-01-01-I1 | Information Disclosure | `.env.example` and docs | medium | mitigate | Commit sample values only, ignore real `.env`, and avoid printing env values in scripts. |
| T-01-01-T1 | Tampering | Workspace script fanout | low | accept | Root scripts are local developer commands; later CI can harden script execution once CI is introduced. |
</threat_model>

<verification>
- FND-01 is partially proven here by workspace/Docker script names; Docker services are implemented in Plan 02.
- FND-03 is proven when `npm run format:check`, `npm run lint`, `npm run typecheck`, `npm run test`, and `npm run verify` are defined and runnable.
- FND-08 is partially proven by `.env.example`; full local setup docs are completed in Plan 03.
- Requirement verification must cite FND-01, FND-03 and FND-08 in the plan summary.
</verification>

<success_criteria>
Plan 01 succeeds when package legitimacy is approved, the npm workspace scaffold exists, root quality scripts exist, the strict TypeScript baseline is in place, and the skeleton contract is RED for the intended missing implementation.
</success_criteria>

<output>
Create `.planning/phases/01-funda-o-t-cnica-e-contrato-visual/01-01-SUMMARY.md` when done.
</output>
