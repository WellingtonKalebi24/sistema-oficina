---
phase: 06-diagn-stico-e-or-amento
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - package-lock.json
  - prisma/schema.prisma
  - prisma/migrations/20260728170000_add_quotes/migration.sql
  - apps/api/src/permissions/permissions.ts
  - prisma/seed.ts
  - apps/api/src/test/testData.ts
autonomous: true
requirements:
  - QTE-01
  - QTE-02
  - QTE-03
  - QTE-04
  - QTE-05
  - QTE-06
  - QTE-07
must_haves:
  truths:
    - "D-01/D-02/D-03/D-04/D-05: Schema supports check-in-origin and direct customer/vehicle quote drafts with problema, causa and recomendacao fields and quote write permission for diagnosis edits."
    - "D-06/D-07: Draft quote persistence uses one item list with service/product kind and item-level plus quote-level discounts and surcharges."
    - "D-08/QTE-06: CompanySetting.quoteDiscountWarningPercent exists with MVP default 10.00 for warning-only discount handling."
    - "D-09/D-10/QTE-07: Validity date and nullable estimated delivery deadline are persisted for later publish validation."
    - "D-16: Quote status persistence can represent Rascunho, Publicado, Enviado, Expirado and Cancelado."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Tenant-scoped Quote and QuoteItem draft persistence plus CompanySetting quote discount warning source."
    - path: "prisma/migrations/20260728170000_add_quotes/migration.sql"
      provides: "Runnable migration for draft quote tables and settings."
    - path: "apps/api/src/permissions/permissions.ts"
      provides: "Quote permission keys for backend route authorization."
  key_links:
    - from: "apps/api/src/permissions/permissions.ts"
      to: "prisma/seed.ts"
      via: "seed derives admin permission data from ALL_PERMISSIONS/PERMISSION_DETAILS."
      pattern: "quotes\\.(read|write|publish|pdf|link|status)"
    - from: "prisma/schema.prisma"
      to: "apps/api/src/test/testData.ts"
      via: "test cleanup removes quote tables before customer/vehicle/reception/catalog fixtures."
      pattern: "quote"
---

<objective>
Create the Phase 6 backend foundation for quote work: approved PDF package metadata, tenant-scoped draft quote schema, quote settings, permissions, seed wiring and test cleanup.

Purpose: This reduces the implementation blast radius before draft quote API work while preserving D-01 through D-10, D-16 and QTE-01 through QTE-07.
Output: Package updates, Prisma schema/migration, permission catalog updates, seed compatibility and fixture cleanup support.
</objective>

## Phase Goal

**As a** workshop operator, **I want to** have tenant-scoped quote data and permissions available, **so that** draft quote APIs can be implemented against stable persistence and authorization contracts.

<execution_context>
@E:/sistema_oficina2/.codex/gsd-core/workflows/execute-plan.md
@E:/sistema_oficina2/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-diagn-stico-e-or-amento/06-CONTEXT.md
@.planning/phases/06-diagn-stico-e-or-amento/06-RESEARCH.md
@.planning/phases/06-diagn-stico-e-or-amento/06-VALIDATION.md
@.planning/phases/06-diagn-stico-e-or-amento/06-PATTERNS.md
@prisma/schema.prisma
@apps/api/package.json
@apps/api/src/permissions/permissions.ts
@prisma/seed.ts
@apps/api/src/test/testData.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install approved PDF dependency and add quote permissions</name>
  <files>apps/api/package.json, package-lock.json, apps/api/src/permissions/permissions.ts, prisma/seed.ts</files>
  <action>Install only audited packages `pdfkit` and, if TypeScript requires it, `@types/pdfkit` from the Phase 6 Package Legitimacy Audit. Add quote permission keys such as `quotes.read`, `quotes.write`, `quotes.publish`, `quotes.pdf`, `quotes.link` and `quotes.status` to `PERMISSIONS`, `ALL_PERMISSIONS` and `PERMISSION_DETAILS`. Keep seed behavior derived from `ALL_PERMISSIONS` so permissions are available to tenant administrators without custom seed branching. This implements D-05 backend quote permission ownership and prepares D-20/D-21 route gates.</action>
  <verify>
    <automated>npm install -w apps/api pdfkit && npm install -D -w apps/api @types/pdfkit</automated>
    <automated>npm run test -w apps/api -- permissions</automated>
  </verify>
  <done>Approved PDF packages are recorded and quote read/write/publish/pdf/link/status permissions are available through the existing seed/catalog flow.</done>
</task>

<task type="auto">
  <name>Task 2: Add draft quote schema, migration and cleanup support</name>
  <files>prisma/schema.prisma, prisma/migrations/20260728170000_add_quotes/migration.sql, apps/api/src/test/testData.ts</files>
  <action>Add `CompanySetting.quoteDiscountWarningPercent` with MVP default `10.00` as the tenant-level source for D-08 warning calculation. Add tenant-scoped `Quote` and `QuoteItem` draft models, plus relation fields from `Tenant`, `Customer`, `Vehicle`, optional `ReceptionCheckIn`, optional `ServiceCatalogEntry` and optional `Product`. Store diagnosis fields as `diagnosisProblema`, `diagnosisCausa` and `diagnosisRecomendacao` per D-04, quote status text with D-16 values, source kind `check-in`/`direct` per D-01, validity date per D-09, nullable estimated delivery deadline per D-10, customer-facing observations, internal notes, quote-level discount/surcharge, calculated totals, `discountWarningPercent` and discount warning metadata per D-07/D-08. Update `testData.ts` cleanup order so quote item/version/link tables introduced in this and later plans are removed before reception/customer/vehicle/catalog rows.</action>
  <verify>
    <automated>npm run db:migrate</automated>
    <automated>npm run test -w apps/api -- prisma-baseline</automated>
  </verify>
  <done>Draft quote persistence and cleanup foundations are migrated and ready for 06-02 API implementation.</done>
</task>

</tasks>

## Artifacts This Phase Produces

- Prisma models: `Quote`, `QuoteItem`.
- Setting: `CompanySetting.quoteDiscountWarningPercent`.
- API constants/types: quote read/write/publish/pdf/link/status permissions.
- Migration: `prisma/migrations/20260728170000_add_quotes/migration.sql`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package-manager-to-repo | Approved npm package metadata enters the API workspace. |
| migration-to-database | Prisma migration creates tenant-scoped quote tables and settings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | npm installs | high | mitigate | Install only packages marked OK in `06-RESEARCH.md` Package Legitimacy Audit; no checkpoint required because no package is `[ASSUMED]`, `[SUS]` or `[SLOP]`. |
| T-06-02 | Tampering | `prisma/schema.prisma` | high | mitigate | Add tenantId foreign keys and indexes for quote/query isolation before service code uses the models. |
| T-06-03 | Elevation of Privilege | `permissions.ts` | high | mitigate | Add explicit quote permission keys so later routes can use backend `requirePermission` instead of frontend-only hiding. |
</threat_model>

<verification>
Run `npm run db:migrate`, `npm run test -w apps/api -- prisma-baseline`, `npm run test -w apps/api -- permissions`, `npm run typecheck -w apps/api` and `npm run lint -w apps/api`.
</verification>

<success_criteria>
Draft quote tables, discount warning setting and quote permissions exist with tenant-aware schema constraints, approved packages and runnable migration checks.
</success_criteria>

<output>
Create `.planning/phases/06-diagn-stico-e-or-amento/06-01-SUMMARY.md` when done.
</output>
