---
phase: 06-diagn-stico-e-or-amento
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - package-lock.json
  - prisma/schema.prisma
  - prisma/migrations/20260728170000_add_quotes/migration.sql
  - apps/api/src/permissions/permissions.ts
  - prisma/seed.ts
  - apps/api/src/test/testData.ts
  - apps/api/src/test/quote-calculator.test.ts
  - apps/api/src/test/quote-contract.test.ts
  - apps/api/src/quotes/quoteSchemas.ts
  - apps/api/src/quotes/quoteCalculator.ts
  - apps/api/src/quotes/quoteService.ts
  - apps/api/src/http/routes/quotes.ts
  - apps/api/src/app.ts
autonomous: true
requirements:
  - QTE-01
  - QTE-02
  - QTE-03
  - QTE-04
  - QTE-05
  - QTE-06
  - QTE-07
must_haves:
  truths:
    - "D-01: Operator can start a quote from a tenant-scoped reception check-in or directly from tenant-scoped customer/vehicle data."
    - "D-02/D-03/D-04/D-05: Check-in-origin quotes require problema, causa and recomendacao before publication eligibility, while direct customer/vehicle quotes may keep diagnosis empty and any quote writer can edit diagnosis."
    - "D-06/D-07: Draft quote items are stored as one list and distinguish service versus product rows while supporting per-item and quote-level discounts and surcharges."
    - "D-08/QTE-06: Above-limit discount produces a persisted warning/audit signal only; save and later publish are not blocked by a missing extra permission."
    - "D-09/D-10/QTE-07: Validity date is required for publish readiness; estimated delivery deadline remains nullable."
    - "QTE-05: Backend totals are deterministic for subtotal, item discounts, item surcharges, quote discount, quote surcharge and final total."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Tenant-scoped Quote and QuoteItem draft persistence plus relations to customer, vehicle, optional check-in, services and products."
    - path: "apps/api/src/quotes/quoteCalculator.ts"
      provides: "Pure quote total calculation."
    - path: "apps/api/src/quotes/quoteService.ts"
      provides: "Tenant-scoped diagnosis, draft quote and item write/read use cases."
    - path: "apps/api/src/http/routes/quotes.ts"
      provides: "Authenticated internal quote JSON routes."
  key_links:
    - from: "apps/api/src/http/routes/quotes.ts"
      to: "apps/api/src/quotes/quoteService.ts"
      via: "route handlers call quote service functions after requirePermission."
      pattern: "createQuote|updateQuoteDraft|listQuotes|getQuote"
    - from: "apps/api/src/quotes/quoteService.ts"
      to: "prisma/schema.prisma"
      via: "Prisma quote and quoteItem delegates with tenantId filters."
      pattern: "prisma\\.(quote|quoteItem)"
    - from: "apps/api/src/quotes/quoteCalculator.ts"
      to: "apps/api/src/test/quote-calculator.test.ts"
      via: "exact expected money totals."
      pattern: "calculateQuoteTotals"
---

<objective>
Create the first usable Phase 6 vertical slice: an authenticated operator can create, edit and list a tenant-scoped diagnosis-backed draft quote with service/product items and backend-calculated totals.

Purpose: This establishes the persisted quote workspace needed by publication, PDF/link and UI work while honoring D-01 through D-10 and QTE-01 through QTE-07.
Output: Quote schema/migration, quote permissions, quote calculator, quote service, quote routes and RED-to-green API tests.
</objective>

## Phase Goal

**As a** workshop operator, **I want to** transform vehicle diagnosis details into a calculated draft quote, **so that** the workshop can prepare a commercial proposal from check-in or direct customer/vehicle data.

<execution_context>
@E:/sistema_oficina2/.codex/gsd-core/workflows/execute-plan.md
@E:/sistema_oficina2/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-diagn-stico-e-or-amento/06-CONTEXT.md
@.planning/phases/06-diagn-stico-e-or-amento/06-RESEARCH.md
@.planning/phases/06-diagn-stico-e-or-amento/06-VALIDATION.md
@.planning/phases/06-diagn-stico-e-or-amento/06-PATTERNS.md
@prisma/schema.prisma
@apps/api/src/permissions/permissions.ts
@prisma/seed.ts
@apps/api/src/test/testData.ts
@apps/api/src/reception/checkInService.ts
@apps/api/src/stock/catalogService.ts
@apps/api/src/stock/stockService.ts
@apps/api/src/http/routes/reception.ts
@apps/api/src/http/routes/stockCatalog.ts
@apps/api/src/app.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED quote draft and calculator contracts</name>
  <files>apps/api/src/test/quote-calculator.test.ts, apps/api/src/test/quote-contract.test.ts, apps/api/src/test/testData.ts</files>
  <read_first>
    <file>.planning/phases/06-diagn-stico-e-or-amento/06-CONTEXT.md</file>
    <file>.planning/phases/06-diagn-stico-e-or-amento/06-VALIDATION.md</file>
    <file>apps/api/src/test/reception-contract.test.ts</file>
    <file>apps/api/src/test/stock-contract.test.ts</file>
    <file>apps/api/src/test/testData.ts</file>
  </read_first>
  <behavior>
    - Test QTE-01/D-01: create diagnosis-backed quote from an existing tenant check-in and create a direct customer/vehicle quote without check-in.
    - Test QTE-02/QTE-04/D-06: add one service item and one product item to one draft quote list and assert serialized kinds allow service/product grouping.
    - Test QTE-05/D-07: exact subtotal, per-item discount/surcharge, quote discount/surcharge and final total values.
    - Test QTE-06/D-08: above-limit discount returns warning metadata and does not return 403.
    - Test QTE-07/D-09/D-10: validity is required for publish readiness; estimated delivery deadline is accepted as null.
  </behavior>
  <action>Create `quote-calculator.test.ts` and `quote-contract.test.ts` before production code. Extend `testData.ts` with FK-safe quote table cleanup placeholders and reusable fixtures for service catalog entries, product rows with stock, reception check-in, customer and vehicle records as needed. Tests should currently fail because quote schema/routes do not exist. Use the established server harness from reception/stock tests, authenticated bearer headers from `loginAs`, and assert cross-tenant linked IDs fail with 400 or 404. Per D-08, do not assert permission blocking for above-limit discount; assert a warning flag/message and successful draft save.</action>
  <verify>
    <automated>npm run test -w apps/api -- quote-calculator quote-contract</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/test/quote-calculator.test.ts` imports `calculateQuoteTotals` from `../quotes/quoteCalculator.js`.
    - `apps/api/src/test/quote-contract.test.ts` exercises `POST /quotes`, `GET /quotes`, `GET /quotes/:quoteId`, and `PATCH /quotes/:quoteId`.
    - RED failure is limited to missing quote modules/routes/schema, not TypeScript syntax errors in the tests.
    - The test text includes no expectations for WhatsApp, email, SMS, push, notification, delivery or read tracking.
  </acceptance_criteria>
  <done>Targeted quote tests exist, encode QTE-01 through QTE-07 and D-01 through D-10, and fail for the expected missing implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Add quote schema, approved PDF dependency and permissions</name>
  <files>apps/api/package.json, package-lock.json, prisma/schema.prisma, prisma/migrations/20260728170000_add_quotes/migration.sql, apps/api/src/permissions/permissions.ts, prisma/seed.ts, apps/api/src/test/testData.ts</files>
  <read_first>
    <file>.planning/phases/06-diagn-stico-e-or-amento/06-RESEARCH.md</file>
    <file>.planning/phases/06-diagn-stico-e-or-amento/06-PATTERNS.md</file>
    <file>apps/api/package.json</file>
    <file>prisma/schema.prisma</file>
    <file>apps/api/src/permissions/permissions.ts</file>
    <file>prisma/seed.ts</file>
    <file>apps/api/src/test/testData.ts</file>
  </read_first>
  <action>Install only audited packages `pdfkit` and, if TypeScript requires it, `@types/pdfkit` from the Phase 6 Package Legitimacy Audit. Add tenant-scoped `Quote` and `QuoteItem` draft models, plus relation fields from `Tenant`, `Customer`, `Vehicle`, optional `ReceptionCheckIn`, optional `ServiceCatalogEntry` and optional `Product`. Store diagnosis fields as `diagnosisProblema`, `diagnosisCausa` and `diagnosisRecomendacao`, quote status text with D-16 values, source kind `check-in`/`direct`, validity date, nullable estimated delivery deadline, customer-facing observations, internal notes, quote-level discount/surcharge, calculated totals and discount warning metadata. Add quote permission keys such as `quotes.read`, `quotes.write`, `quotes.publish`, `quotes.pdf`, `quotes.link` and `quotes.status` to `PERMISSIONS`, `ALL_PERMISSIONS` and `PERMISSION_DETAILS`; seed remains derived from `ALL_PERMISSIONS`. Update `testData.ts` cleanup order so quote item/version/link tables introduced in this and later plans are removed before reception/customer/vehicle/catalog rows.</action>
  <verify>
    <automated>npm run db:migrate</automated>
    <automated>npm run test -w apps/api -- prisma-baseline quote-calculator quote-contract</automated>
  </verify>
  <acceptance_criteria>
    - `[BLOCKING]` `npm run db:migrate` runs after schema/migration edits and before quote API verification.
    - `prisma/schema.prisma` contains `model Quote` and `model QuoteItem` with `tenantId` and tenant/date/status indexes.
    - The migration creates quote tables with tenant/customer/vehicle/check-in/catalog/product foreign keys and decimal money columns.
    - `apps/api/src/permissions/permissions.ts` contains quote permissions in `PERMISSIONS`, `ALL_PERMISSIONS` and `PERMISSION_DETAILS`.
    - `apps/api/package.json` and `package-lock.json` include only the audited PDF package names required by this phase.
  </acceptance_criteria>
  <done>Database and permission foundation for draft quotes is migrated, seeded through existing permission flow, and ready for service implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement tenant-scoped draft quote API</name>
  <files>apps/api/src/quotes/quoteSchemas.ts, apps/api/src/quotes/quoteCalculator.ts, apps/api/src/quotes/quoteService.ts, apps/api/src/http/routes/quotes.ts, apps/api/src/app.ts, apps/api/src/test/quote-calculator.test.ts, apps/api/src/test/quote-contract.test.ts</files>
  <read_first>
    <file>apps/api/src/stock/stockSchemas.ts</file>
    <file>apps/api/src/reception/checkInService.ts</file>
    <file>apps/api/src/tenancy/tenantScope.ts</file>
    <file>apps/api/src/audit/auditService.ts</file>
    <file>apps/api/src/http/routes/reception.ts</file>
    <file>apps/api/src/http/routes/stockCatalog.ts</file>
    <file>apps/api/src/app.ts</file>
  </read_first>
  <action>Create Zod schemas for quote create/update/list inputs, item kinds, D-16 statuses used in this plan, dates, quantities and money strings. Implement `calculateQuoteTotals` as a pure backend utility using deterministic cents or Prisma Decimal boundaries and exact string output. Implement `quoteService.ts` with `listQuotes`, `getQuote`, `createQuote`, `updateQuoteDraft` and serializers. Validate every linked customer, vehicle, check-in, service and product belongs to `actor.tenantId`; check-in-origin quotes must link to the same customer/vehicle as the check-in. Snapshot item description and sale/base price into `QuoteItem` at draft item creation. Persist above-limit discount warning metadata and write concise audit rows for quote creation, diagnosis update and draft item/value edits without raw long notes. Mount `createQuotesRouter(prisma)` in `app.ts` after `requireAuth`, protect read routes with `quotes.read` and mutation routes with `quotes.write`, and keep public approval routes out of Phase 6.</action>
  <verify>
    <automated>npm run test -w apps/api -- quote-calculator quote-contract</automated>
    <automated>npm run typecheck -w apps/api</automated>
    <automated>npm run lint -w apps/api</automated>
  </verify>
  <acceptance_criteria>
    - `POST /quotes` creates check-in-origin and direct-origin draft quotes under the authenticated tenant.
    - `PATCH /quotes/:quoteId` edits draft diagnosis, commercial fields and items while blocking foreign tenant/customer/vehicle/check-in/product/service IDs.
    - Response DTOs include grouped-capable item `kind`, snapshot description, quantity, unit price, item adjustments, totals and `discountWarning`.
    - Above-limit discount saves successfully and emits an audit-visible warning signal per D-08.
    - No route, service, model or audit action sends customer messages, opens external messaging URLs, or records delivery/read communication state.
  </acceptance_criteria>
  <done>QTE-01 through QTE-07 pass through authenticated API tests with backend-authoritative tenant scope, permissions, validation, totals and audit.</done>
</task>

</tasks>

## Artifacts This Phase Produces

- Prisma models: `Quote`, `QuoteItem`.
- API constants/types: `PERMISSIONS.quotesRead`, `PERMISSIONS.quotesWrite`, `PERMISSIONS.quotesPublish`, `PERMISSIONS.quotesPdf`, `PERMISSIONS.quotesLink`, `PERMISSIONS.quotesStatus`.
- API modules: `apps/api/src/quotes/quoteSchemas.ts`, `apps/api/src/quotes/quoteCalculator.ts`, `apps/api/src/quotes/quoteService.ts`, `apps/api/src/http/routes/quotes.ts`.
- API functions: `calculateQuoteTotals`, `listQuotes`, `getQuote`, `createQuote`, `updateQuoteDraft`, `createQuotesRouter`.
- API routes: `GET /quotes`, `POST /quotes`, `GET /quotes/:quoteId`, `PATCH /quotes/:quoteId`.
- Tests: `apps/api/src/test/quote-calculator.test.ts`, `apps/api/src/test/quote-contract.test.ts`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser-to-API | Authenticated operator input crosses into quote create/update/list routes. |
| API-to-database | Backend writes tenant-scoped quote rows linked to customer, vehicle, check-in and catalog records. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | `updateQuoteDraft` | high | mitigate | Reject updates to non-draft quotes and validate every linked ID against `actor.tenantId` before writing. |
| T-06-02 | Information Disclosure | `createQuote` and `getQuote` | high | mitigate | Use tenantId filters and existing tenant-scope helpers so foreign tenant IDs return 400/404. |
| T-06-03 | Repudiation | diagnosis and draft commercial edits | medium | mitigate | Write sanitized audit rows with tenantId, userId, action, entity, recordId and changed field names. |
| T-06-04 | Tampering | quote totals | medium | mitigate | Recalculate totals in `quoteCalculator.ts` on the backend and persist calculated values instead of trusting client totals. |
| T-06-SC | Tampering | npm installs | high | mitigate | Install only packages marked OK in `06-RESEARCH.md` Package Legitimacy Audit; no checkpoint required because no package is `[ASSUMED]`, `[SUS]` or `[SLOP]`. |
</threat_model>

<verification>
Run `npm run db:migrate`, `npm run test -w apps/api -- quote-calculator quote-contract`, `npm run typecheck -w apps/api` and `npm run lint -w apps/api`.
</verification>

<success_criteria>
The authenticated API can create and edit draft quotes from check-in or direct customer/vehicle entry points, calculate totals exactly, warn without blocking above-limit discounts, enforce tenant/permission boundaries and audit critical draft actions.
</success_criteria>

<output>
Create `.planning/phases/06-diagn-stico-e-or-amento/06-01-SUMMARY.md` when done.
</output>
