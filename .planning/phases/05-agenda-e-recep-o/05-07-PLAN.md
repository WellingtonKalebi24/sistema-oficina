---
phase: 05-agenda-e-recepcao
plan: 07
type: execute
wave: 7
depends_on:
  - 05-06
files_modified:
  - package-lock.json
  - apps/api/package.json
  - apps/api/src/types/multer.d.ts
  - apps/api/src/config/env.ts
  - compose.yaml
  - .env.example
  - prisma/schema.prisma
  - prisma/migrations/20260724110000_add_checkin_attachments/migration.sql
  - apps/api/src/permissions/permissions.ts
autonomous: true
requirements:
  - REC-05
  - REC-07
  - REC-08
user_setup: []
must_haves:
  truths:
    - "D-08: Attachments are optional and schema does not make them part of check-in completion."
    - "D-09: Attachment categories are Avaria, Documento, Painel, Motor, Interior, Outro."
    - "D-11: Attachment model includes tenant/check-in ownership for backend permission checks."
  artifacts:
    - "prisma/migrations/20260724110000_add_checkin_attachments/migration.sql"
    - "apps/api/src/types/multer.d.ts"
  key_links:
    - "multer OK install -> upload env root -> CheckInAttachment schema -> attachment permissions"
---

<objective>
Preparar dependencias, configuracao e schema de anexos.

Purpose: Separar storage/config/schema do servico de anexos para manter o plano abaixo do limite de escopo.
Output: `multer` instalado, env/Compose de upload root, migration CheckInAttachment e permissoes.
</objective>

<execution_context>
@E:/sistema_oficina2/.codex/gsd-core/workflows/execute-plan.md
@E:/sistema_oficina2/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-agenda-e-recep-o/05-RESEARCH.md
@.planning/phases/05-agenda-e-recep-o/05-CONTEXT.md
@.planning/phases/05-agenda-e-recep-o/05-VALIDATION.md
@.planning/phases/05-agenda-e-recep-o/05-06-SUMMARY.md
@apps/api/src/config/env.ts
@compose.yaml
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install approved upload dependency and config</name>
  <files>package-lock.json, apps/api/package.json, apps/api/src/types/multer.d.ts, apps/api/src/config/env.ts, compose.yaml, .env.example</files>
  <action>Install only `multer` in `apps/api`; RESEARCH.md Package Legitimacy Audit marks it OK. Do not install `@types/multer` because it is marked SUS. If TypeScript needs declarations, add a narrow local `apps/api/src/types/multer.d.ts` for the used API only. Add `RECEPTION_UPLOAD_ROOT=uploads/reception` to env example and env parsing, and wire Docker Compose storage for the API service without public static serving.</action>
  <verify>
    <automated>npm install multer -w apps/api ; npm run typecheck -w apps/api ; npm run docker:config</automated>
  </verify>
  <done>`multer` is installed, no SUS type package is installed, env/config compile and Compose config is valid.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add attachment schema and permissions</name>
  <files>prisma/schema.prisma, prisma/migrations/20260724110000_add_checkin_attachments/migration.sql, apps/api/src/permissions/permissions.ts</files>
  <behavior>
    - CheckInAttachment stores tenantId, checkInId, category, originalName, storedName, relativePath, mimeType, sizeBytes, uploadedByUserId, deletedAt and deletedByUserId.
    - Categories persist D-09 canonical values.
    - Permission catalog includes read/write/delete attachment keys.
  </behavior>
  <action>Add `CheckInAttachment` model with tenant/check-in indexes and soft-delete fields. Add `reception.attachments.read`, `reception.attachments.write` and `reception.attachments.delete`. Ensure deletion metadata remains tenant-scoped and available for audit while deleted attachments will be non-downloadable in later service.</action>
  <verify>
    <automated>npm run db:migrate ; npm run typecheck -w apps/api</automated>
  </verify>
  <done>Attachment schema, permissions and config are ready for protected routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> API workspace | New package enters runtime dependency graph. |
| API -> filesystem | Upload root configuration determines where bytes will be written later. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-20 | Tampering | npm install | high | mitigate | Install only approved `multer`; do not install SUS `@types/multer`. |
| T-05-21 | Information Disclosure | upload root config | high | mitigate | Configure storage root without adding public static route. |
| T-05-22 | Repudiation | deleted metadata | medium | mitigate | Schema keeps deletedAt/deletedByUserId for audit-safe history. |
| T-05-SC | Tampering | npm installs | high | mitigate | Package Legitimacy Audit exists and forbids SUS install without checkpoint. |
</threat_model>

<verification>
Run approved install, API typecheck, migration and Docker config.
</verification>

<success_criteria>
Attachment backend prerequisites are installed/configured with no public exposure and no SUS package.
</success_criteria>

<output>
Create `.planning/phases/05-agenda-e-recep-o/05-07-SUMMARY.md` when done.
</output>
