---
phase: 05-agenda-e-recepcao
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - apps/api/src/reception/receptionSchemas.ts
  - apps/api/src/reception/appointmentService.ts
  - apps/api/src/http/routes/reception.ts
  - apps/api/src/app.ts
  - apps/api/src/test/reception-contract.test.ts
autonomous: true
requirements:
  - REC-01
  - REC-02
  - REC-07
  - REC-08
user_setup: []
must_haves:
  truths:
    - "REC-01: Authenticated API creates, edits, cancels and lists appointments by day/week."
    - "REC-02/D-05: Backend validates customer and vehicle tenant ownership before appointment mutation."
    - "REC-08: Appointment create/update/cancel writes audit rows."
  artifacts:
    - "apps/api/src/reception/appointmentService.ts"
    - "apps/api/src/http/routes/reception.ts"
  key_links:
    - "requireAuth -> createReceptionRouter -> requirePermission -> appointmentService -> Prisma tenantId filters"
---

<objective>
Implementar a API protegida de agenda sobre o schema criado em 05-01.

Purpose: Tornar REC-01/REC-02 utilizaveis no backend, com tenant isolation e auditoria antes da UI.
Output: Schemas Zod, appointmentService, rotas `/reception/appointments` e montagem em `app.ts`.
</objective>

<execution_context>
@E:/sistema_oficina2/.codex/gsd-core/workflows/execute-plan.md
@E:/sistema_oficina2/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-agenda-e-recep-o/05-CONTEXT.md
@.planning/phases/05-agenda-e-recep-o/05-RESEARCH.md
@.planning/phases/05-agenda-e-recep-o/05-01-SUMMARY.md
@apps/api/src/app.ts
@apps/api/src/tenancy/tenantScope.ts
@apps/api/src/audit/auditService.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement appointment service and schemas</name>
  <files>apps/api/src/reception/receptionSchemas.ts, apps/api/src/reception/appointmentService.ts, apps/api/src/test/reception-contract.test.ts</files>
  <behavior>
    - Zod schemas validate date ranges, status values and required customer/vehicle IDs.
    - Service derives tenantId from authenticated actor, validates customer/vehicle ownership per D-05 and writes audit rows.
    - Day/week list uses tenantId and startsAt ordering for D-12/D-17.
  </behavior>
  <action>Create reception schemas and appointment service functions for create, update, cancel, daily list and weekly list. Use existing tenant helper patterns from customer/vehicle services. Keep status transitions limited to Agendado, Cancelado and Convertido; check-in conversion itself is implemented later. Audit concise changed fields without long notes.</action>
  <verify>
    <automated>npm run test -w apps/api -- reception-contract</automated>
  </verify>
  <done>Service behavior satisfies appointment contract tests except route mounting if still pending.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mount protected reception appointment routes</name>
  <files>apps/api/src/http/routes/reception.ts, apps/api/src/app.ts</files>
  <behavior>
    - Routes live under `/reception/appointments` after `requireAuth`.
    - Read/write/cancel permissions are enforced by backend middleware.
    - Errors use existing async/error handling and do not leak Prisma details.
  </behavior>
  <action>Create `createReceptionRouter(prisma)` and mount it in `apps/api/src/app.ts` with existing protected router conventions. Add GET day/week filters, POST create, PATCH edit and POST cancel or DELETE-equivalent cancel endpoint matching the tests. Do not add any customer communication route or notification concept.</action>
  <verify>
    <automated>npm run test -w apps/api -- reception-contract ; npm run typecheck -w apps/api ; npm run lint -w apps/api</automated>
  </verify>
  <done>Appointment API tests pass for create/edit/cancel/day/week, tenant validation and audit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/API client -> /reception/appointments | Untrusted filters and appointment payloads cross into protected backend. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Spoofing | reception routes | high | mitigate | Mount routes after `requireAuth`; never accept actor identity from body. |
| T-05-04 | Tampering | appointmentService | high | mitigate | Use Zod validation and tenant-owned customer/vehicle checks per D-05. |
| T-05-05 | Repudiation | appointment audit | medium | mitigate | Audit create/update/cancel with tenantId, userId, entity and recordId. |
| T-05-06 | Information Disclosure | appointment list/detail | high | mitigate | Every query filters by authenticated tenantId. |
</threat_model>

<verification>
`npm run test -w apps/api -- reception-contract`, API typecheck and API lint must pass for appointment API behavior.
</verification>

<success_criteria>
Backend agenda is operational and authoritative for REC-01, REC-02, REC-07, REC-08 and D-05.
</success_criteria>

<output>
Create `.planning/phases/05-agenda-e-recep-o/05-02-SUMMARY.md` when done.
</output>
