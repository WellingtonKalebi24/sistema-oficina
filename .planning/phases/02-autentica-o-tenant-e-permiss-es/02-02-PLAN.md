---
phase: 02-autentica-o-tenant-e-permiss-es
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/*/migration.sql
  - prisma/seed.ts
  - apps/api/src/test/testData.ts
  - apps/api/src/test/prisma-baseline.test.ts
  - apps/api/src/test/auth-bootstrap.test.ts
  - apps/api/src/test/auth-sessions.test.ts
autonomous: true
requirements:
  - IDT-01
  - IDT-03
  - IDT-04
  - IDT-05
  - IDT-06
  - IDT-07
  - IDT-10
must_haves:
  truths:
    - "D-01: Identity schema supports first tenant/admin bootstrap and company settings."
    - "D-05 and D-06: Configurable roles, permissions and user-specific overrides exist in the migration."
    - "D-08, D-09 and D-10: Session records support multi-session refresh rotation and current-session revocation."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Tenant, CompanySetting, User, Role, Permission, RolePermission, UserRole, UserPermissionOverride, Session, PasswordResetToken and AuditLog models."
      contains: "model Tenant"
    - path: "prisma/migrations/*/migration.sql"
      provides: "Versioned Phase 2 identity and permission migration."
    - path: "apps/api/src/test/testData.ts"
      provides: "Reusable tenant, admin, user, session, permission and audit test fixtures."
  key_links:
    - from: "prisma/schema.prisma"
      to: "apps/api/src/test/prisma-baseline.test.ts"
      via: "Schema baseline expects Phase 2 identity models and rejects out-of-scope business/communication models"
      pattern: "model Tenant"
---

<objective>
Create the Phase 2 database contract and RED fixture/test foundation for identity, sessions and configurable permissions.

Purpose: Later auth and admin plans need stable Prisma models and test helpers before writing route/service code.
Output: Versioned migration, deterministic seed updates, baseline schema tests, and failing bootstrap/session tests.
</objective>

<execution_context>
@E:/sistema_oficina2/.codex/gsd-core/workflows/execute-plan.md
@E:/sistema_oficina2/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-autentica-o-tenant-e-permiss-es/02-CONTEXT.md
@.planning/phases/02-autentica-o-tenant-e-permiss-es/02-RESEARCH.md
@.planning/phases/02-autentica-o-tenant-e-permiss-es/02-VALIDATION.md
@.planning/phases/02-autentica-o-tenant-e-permiss-es/02-PATTERNS.md
@prisma/schema.prisma
@prisma/seed.ts
@apps/api/src/test/app.test.ts
@apps/api/src/test/prisma-baseline.test.ts
</context>

## Phase Goal

**As a** workshop administrator, **I want to** access JO.IA securely with tenant-aware identity and permissions, **so that** each workshop's users, settings and administrative actions stay isolated and auditable.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED schema and fixture tests</name>
  <files>apps/api/src/test/testData.ts, apps/api/src/test/prisma-baseline.test.ts, apps/api/src/test/auth-bootstrap.test.ts, apps/api/src/test/auth-sessions.test.ts</files>
  <behavior>
    - `prisma-baseline.test.ts` expects identity/tenant/session/permission/audit models and still rejects customer, vehicle, stock, quote, work order, finance, notification, WhatsApp, SMS and customer communication entities.
    - Bootstrap tests describe D-01: first admin creates tenant and company settings, then second bootstrap attempt returns 409.
    - Session tests describe D-08, D-09 and D-10: two sessions can coexist, refresh rotates only the active session, and logout revokes only the current session.
  </behavior>
  <action>Create RED tests and reusable helpers before production schema changes. `testData.ts` must expose helpers such as `resetIdentityTables`, `createTenantWithAdmin`, `createUserWithRole`, `loginAs`, and `getAuditPayloads`, following the injected Prisma/server patterns from Phase 1 tests. The tests should fail because Prisma models and auth routes do not exist yet, not because of malformed setup.</action>
  <verify>
    <automated>npm run test -w apps/api -- prisma-baseline auth-bootstrap auth-sessions</automated>
  </verify>
  <done>RED coverage exists for IDT-01, IDT-05, IDT-06, IDT-07 and IDT-10 schema/session expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement identity schema, seed and migration</name>
  <files>prisma/schema.prisma, prisma/migrations/*/migration.sql, prisma/seed.ts</files>
  <behavior>
    - Schema contains tenant-scoped models for company settings, users, roles, permission grants, user overrides, sessions, reset tokens and audit logs.
    - Stable dot-key permissions include `tenant.settings.read`, `tenant.settings.update`, `users.read`, `users.create`, `users.update`, `users.deactivate`, `users.createAdmin`, `roles.manage`, `permissions.manage`, and `audit.read`.
    - Session stores only a hashed opaque refresh token secret, revocation metadata and per-session audit metadata.
  </behavior>
  <action>Add the full Phase 2 Prisma model set in one schema change so later plans do not need schema churn. Use snake_case table maps, explicit timestamps, tenant indexes, compound uniqueness where needed, and relations that let service code validate tenant ownership. Update seed data with deterministic permission keys and default role metadata only; do not create customer, vehicle, stock, quote, finance, notification or customer communication entities. Run `npm run db:migrate` to create the versioned migration.</action>
  <verify>
    <automated>npm run db:migrate && npm run test -w apps/api -- prisma-baseline auth-bootstrap auth-sessions</automated>
  </verify>
  <done>The migration exists, schema baseline passes, and auth tests are ready for Plan 03 service/route implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API to PostgreSQL | Identity, session, reset and permission records become persistent tenant data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02-01 | Information Disclosure | tenant-scoped schema | high | mitigate | Tenant IDs, indexes and relation constraints are modeled before service code; tests reject cross-scope business leakage. |
| T-02-02-02 | Tampering | migrations | medium | mitigate | Use Prisma-generated versioned migration through `npm run db:migrate`, not ad hoc database edits. |
| T-02-02-SC | Tampering | npm installs | high | mitigate | Depends on completed Plan 02-01 package gate; this plan installs no packages. |
</threat_model>

<verification>
Run `npm run db:migrate && npm run test -w apps/api -- prisma-baseline auth-bootstrap auth-sessions`.
</verification>

<success_criteria>
The database contract supports every Phase 2 identity and permission behavior, the migration is executable, and RED auth/session tests are available for the next plan.
</success_criteria>

<output>
Create `.planning/phases/02-autentica-o-tenant-e-permiss-es/02-02-SUMMARY.md` when done.
</output>
