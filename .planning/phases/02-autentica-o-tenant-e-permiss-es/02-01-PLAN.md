---
phase: 02-autentica-o-tenant-e-permiss-es
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - apps/api/package.json
  - apps/web/package.json
autonomous: false
requirements:
  - IDT-05
  - IDT-06
  - IDT-08
must_haves:
  truths:
    - "Approved authentication packages are installed before auth, session and recovery code is written."
    - "SUS packages from 02-RESEARCH.md are verified by a human before installation."
  artifacts:
    - path: "apps/api/package.json"
      provides: "API dependencies for Argon2id passwords, jose JWTs, Zod validation, SMTP recovery mail and rate limiting."
      contains: "argon2"
    - path: "apps/web/package.json"
      provides: "Web dependency for authenticated admin route surfaces."
      contains: "react-router"
  key_links:
    - from: "02-RESEARCH.md Package Legitimacy Audit"
      to: "apps/api/package.json"
      via: "Human package gate before installing nodemailer and express-rate-limit"
      pattern: "nodemailer|express-rate-limit"
    - from: "02-RESEARCH.md Package Legitimacy Audit"
      to: "apps/web/package.json"
      via: "Human package gate before installing react-router"
      pattern: "react-router"
---

<objective>
Prepare the dependency surface for Phase 2 authentication, session, password recovery, permission validation and admin routing.

Purpose: Phase 2 needs vetted security libraries before backend and UI slices can be implemented. This plan exists only to satisfy the package legitimacy gate; implementation work is intentionally in later plans.
Output: Updated workspace package manifests and lockfile with approved auth/admin dependencies.
</objective>

<execution_context>
@E:/sistema_oficina2/.codex/gsd-core/workflows/execute-plan.md
@E:/sistema_oficina2/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-autentica-o-tenant-e-permiss-es/02-CONTEXT.md
@.planning/phases/02-autentica-o-tenant-e-permiss-es/02-RESEARCH.md
@.planning/phases/02-autentica-o-tenant-e-permiss-es/02-PATTERNS.md
@package.json
@apps/api/package.json
@apps/web/package.json
</context>

## Phase Goal

**As a** workshop administrator, **I want to** access JO.IA securely with tenant-aware identity and permissions, **so that** each workshop's users, settings and administrative actions stay isolated and auditable.

## Artifacts this phase produces

- Prisma models: `Tenant`, `CompanySetting`, `User`, `Role`, `Permission`, `RolePermission`, `UserRole`, `UserPermissionOverride`, `Session`, `PasswordResetToken`, `AuditLog`.
- Prisma migration under `prisma/migrations/*`.
- API modules: `auth/passwords.ts`, `auth/tokens.ts`, `auth/sessions.ts`, `auth/passwordReset.ts`, `permissions/permissions.ts`, `permissions/permissionService.ts`, `audit/auditService.ts`, `mail/emailSender.ts`, `tenancy/tenantScope.ts`, `http/middleware/requireAuth.ts`, `http/middleware/requirePermission.ts`.
- API routes: `http/routes/bootstrap.ts`, `http/routes/auth.ts`, `http/routes/tenantSettings.ts`, `http/routes/users.ts`, `http/routes/roles.ts`.
- Environment variables: `JWT_ACCESS_SECRET`, `JWT_ISSUER`, `JWT_AUDIENCE`, `ACCESS_TOKEN_TTL_SECONDS`, `REFRESH_TOKEN_TTL_DAYS`, `PASSWORD_RESET_TTL_MINUTES`, `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS`, `SMTP_FROM`, `AUTH_RATE_LIMIT_WINDOW_MS`, `AUTH_RATE_LIMIT_MAX`.
- Web modules: `api/auth.ts`, `api/admin.ts`, `auth/session.ts`, authenticated `App.tsx` shell, updated `styles.css`, `test/auth-ui.test.tsx`.
- Tests: `auth-bootstrap.test.ts`, `auth-sessions.test.ts`, `permissions.test.ts`, `tenant-isolation.test.ts`, `audit.test.ts`, `testData.ts`, updated `prisma-baseline.test.ts`.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify SUS package legitimacy before install</name>
  <read_first>
    - `.planning/phases/02-autentica-o-tenant-e-permiss-es/02-RESEARCH.md`
    - `package.json`
    - `apps/api/package.json`
    - `apps/web/package.json`
  </read_first>
  <files>package.json, package-lock.json, apps/api/package.json, apps/web/package.json</files>
  <action>Pause before installing `nodemailer`, `express-rate-limit`, or `react-router`. The executor must present the package verification steps below and proceed only after the user approves these exact package names. This implements the 02-RESEARCH.md package checkpoint requirement for the SUS packages.</action>
  <what-built>Package legitimacy audit in `02-RESEARCH.md` flagged `nodemailer`, `express-rate-limit`, and `react-router` as SUS due recent latest publishes.</what-built>
  <how-to-verify>
    1. Open `https://www.npmjs.com/package/nodemailer`, confirm the package name, publisher/source repo, age and download profile match the research audit.
    2. Open `https://www.npmjs.com/package/express-rate-limit`, confirm the package name, publisher/source repo, age and download profile match the research audit.
    3. Open `https://www.npmjs.com/package/react-router`, confirm the package name, publisher/source repo, age and download profile match the research audit.
    4. Approve only the exact package names above. Do not approve similarly named packages.
  </how-to-verify>
  <verify>
    <automated>npm ls -w apps/api nodemailer express-rate-limit; npm ls -w apps/web react-router</automated>
  </verify>
  <resume-signal>Type "approved" to install these exact packages, or describe the package concern.</resume-signal>
  <acceptance_criteria>Human approval is recorded before any SUS dependency is installed.</acceptance_criteria>
  <done>The user has approved the exact SUS packages or execution is blocked before package installation.</done>
</task>

<task type="auto">
  <name>Task 2: Install approved Phase 2 dependencies</name>
  <read_first>
    - `package.json`
    - `apps/api/package.json`
    - `apps/web/package.json`
    - `.planning/phases/02-autentica-o-tenant-e-permiss-es/02-RESEARCH.md`
  </read_first>
  <files>package.json, package-lock.json, apps/api/package.json, apps/web/package.json</files>
  <action>After Task 1 approval, run `npm install -w apps/api argon2 jose zod nodemailer express-rate-limit` and `npm install -w apps/web react-router`. Use the exact package names from 02-RESEARCH.md. Do not install alternative similarly named packages. Keep `argon2`, `jose`, and `zod` as the approved core libraries for IDT-05, IDT-06 and IDT-08. Use `nodemailer` only for authentication password recovery per D-11, D-12, D-13 and D-14; do not create customer communication modules, notification centers or message queues.</action>
  <verify>
    <automated>npm ls -w apps/api argon2 jose zod nodemailer express-rate-limit && npm ls -w apps/web react-router && npm run typecheck --workspaces --if-present</automated>
  </verify>
  <acceptance_criteria>`apps/api/package.json`, `apps/web/package.json` and `package-lock.json` contain only the approved package additions, with no unrelated dependency churn.</acceptance_criteria>
  <done>Dependency installation is complete and workspace type checks still run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry to local workspace | Third-party code enters the repository through package installation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01-SC | Tampering | npm installs | high | mitigate | Blocking human checkpoint verifies `nodemailer`, `express-rate-limit`, and `react-router` before installation; exact names only. |
| T-02-01-01 | Spoofing | package names | medium | mitigate | Task 2 installs exact audited names and rejects similarly named packages. |
</threat_model>

<verification>
Run `npm ls -w apps/api argon2 jose zod nodemailer express-rate-limit`, `npm ls -w apps/web react-router`, and `npm run typecheck --workspaces --if-present`.
</verification>

<success_criteria>
The dependency gate is approved, package manifests are updated, no unapproved packages are present, and later Phase 2 implementation plans can import the approved dependencies.
</success_criteria>

<output>
Create `.planning/phases/02-autentica-o-tenant-e-permiss-es/02-01-SUMMARY.md` when done.
</output>
